# Uplink runtime secrets — safe to commit (1Password references only).
# Use: op run --env-file=scripts/uplink.op.env -- npm run dev
#
# Add to vault when available:
#   GMAIL_APP_PASSWORD=op://phqzta7t7exkddtreg5o35jdxy/<item>/credential
#
# Turnstile production keys: run scripts/create-turnstile-widget.ps1 (requires Turnstile:Edit on token),
# then set NEXT_PUBLIC_TURNSTILE_SITE_KEY and TURNSTILE_SECRET_KEY in Vercel.
#
# Local dev may use Cloudflare always-pass test keys in .env.local (see Turnstile testing docs).

GMAIL_USER=viral.architect@gmail.com
