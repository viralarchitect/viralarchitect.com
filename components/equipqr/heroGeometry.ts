/**
 * Center vertical line in the 100×100 hero stage viewBox.
 * Used as the morph start path and as the Suspense loading placeholder so
 * cold-load chunk waits never flash a state outline (e.g. Texas).
 */
export const HERO_VERTICAL_LINE = "M 50 0 L 50 100 Z";
